{
  "name": "JsonLang",
  "scripts": {
    "bootstrap": "npm install && lerna exec npm install",
    "build": "lerna run build",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "start": "lerna exec npm run --parallel watch"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.1.0"
  }
}
